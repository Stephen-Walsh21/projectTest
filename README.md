# Voice-driven Personal Assistant

voice-driven assistant features:

- Set reminders / alarms 
- Text-to-speech via 
- Respond to voice commands (uses SpeechRecognition with Google recognizer)
- Analyze a schedule and propose a simple fitness plan

```
Create a virtual environment and install dependencies:
```
```
On Linux, install PortAudio for `pyaudio` (Debian/Ubuntu):
```
```
- Voice input may require a working microphone and drivers; if voice fails, the CLI falls back to typed commands.
```